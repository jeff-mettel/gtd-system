// The ledger package is the single source of schema, validation and fold for every writer.
export { fold, validate, upcast, newId, demoEvents, LEDGER_VERSION } from '../packages/ledger/index.js';
export const LEDGER_SOURCE = 'packages/ledger';
